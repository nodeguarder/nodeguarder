name: Sync ATR Rules
on:
  schedule:
    - cron: '0 6 * * 1'  # Weekly Monday 6am UTC
  workflow_dispatch:       # Manual trigger

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Clone ATR rules (rules only)
        run: |
          git init atr-rules
          cd atr-rules
          git remote add origin https://github.com/Agent-Threat-Rule/agent-threat-rules.git
          git config core.protectNTFS false
          git fetch --depth 1 origin main
          git sparse-checkout set rules
          git checkout -b main origin/main

      - name: Install deps
        run: pip install pyyaml

      - name: Sync rules
        run: python scripts/sync_atr_rules.py --atr-dir $PWD/atr-rules --output agent/atr_rules.json

      - name: Check for changes
        id: diff
        run: |
          if git diff --quiet agent/atr_rules.json; then
            echo "changed=false" >> $GITHUB_OUTPUT
          else
            echo "changed=true" >> $GITHUB_OUTPUT
          fi

      - name: Commit and push
        if: steps.diff.outputs.changed == 'true'
        run: |
          git config user.name "atr-bot[bot]"
          git config user.email "atr-bot@users.noreply.github.com"
          git add agent/atr_rules.json
          git commit -m "sync ATR rules ($(date +%Y-%m-%d))" -m "Automated sync from Agent-Threat-Rule/agent-threat-rules"
          git push
